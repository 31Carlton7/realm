# Hugeicons Pro
Icons come from `@hugeicons/react` + `@hugeicons-pro/core-stroke-rounded`.
The Pro registry needs a token: put the registry + token lines in `~/.npmrc` (user-level) or in a `.npmrc` at repo root (see `.npmrc.example`); never commit the repo `.npmrc`.
